name: dart_example
description: "A dart-web example for wasm_ffi"
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  web: ^1.0.0
  wasm_ffi:
    path: ../
  example:
    path: ../example

dev_dependencies:
  build_runner: ^2.4.8
  build_web_compilers: ^4.0.9
  lints: ^4.0.0
